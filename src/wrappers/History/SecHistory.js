import React from 'react';
import { Stack , Typography } from '@mui/material';

const SecHistory = () => {
    return (
        <Stack spacing={1} sx={{mt:3}}>
              <Typography variant='h4'>History</Typography>
        </Stack>
    )
}

export default SecHistory