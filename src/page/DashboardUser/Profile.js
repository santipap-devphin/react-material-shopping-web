import React  from "react";
import MainBlock from "../../wrappers/MainBlock/MainBlock";
import SecMainProfile from "../../wrappers/Profile/SecMainProfile";

const Profile = () => {

   return (<MainBlock titlepage={"ข้อมูลส่วนตัว"}>
                <SecMainProfile/>
         </MainBlock>
    )
}

export default Profile